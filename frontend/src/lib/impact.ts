export function estimateImpact(replacementCost: number | null, transferCost: number | null, massKg: number | null, quantity: number) { return { procurementAvoided: replacementCost === null ? null : replacementCost * quantity, costDifference: replacementCost === null || transferCost === null ? null : (replacementCost - transferCost) * quantity, wasteAvoided: massKg === null ? null : massKg * quantity, status: "ESTIMATED" as const, assumptions: ["Replacement cost and transfer cost are supplied estimates.", "Waste avoidance is mass-based and is not a carbon claim."] }; }
